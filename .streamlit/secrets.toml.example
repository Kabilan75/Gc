# Copy values into `.streamlit/secrets.toml` locally (gitignored) or paste into
# Streamlit Community Cloud → App settings → Secrets.

# Optional: HTTPS URL to your Combined Data workbook (sheet `Combined Data`).
# Server fetches it once per cache window — no browser upload required on Cloud.
# GLOBAL_COMBINED_WORKBOOK_URL = "https://your-host.example/path/Updated_27_02_26_-_Kabilan.xlsx"
